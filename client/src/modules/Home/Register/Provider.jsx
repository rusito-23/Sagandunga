import React, {Component} from 'react';
import '../Form.scss'

export default class RegisterProvider extends Component {
    render() {
        return (
            <div className={'Form Form-register'}><p>PROVIDER</p></div>
        );
    }
}